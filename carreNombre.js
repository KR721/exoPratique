//  Renvoie le carré d'un nombre
function carre(x) {
//TODO : compléter le code de la fonction
	var produit = x*x ;
	return produit;
}
	var nombre=Number(prompt("Saisissez un nombre !"));
   // while (nombre != 0){
    if (nombre = "0"){
		console.log (" Le carre de " + nombre + " est : " + carre(nombre));
	}
	if (nombre = "2"){
		console.log (" Le carre de " + nombre + " est : " + carre(nombre));
	}
    if (nombre = "5"){
        console.log(" Le carre de " + nombre + " est : " + carre(nombre));
    }
    
    else { 
      console.log ("Recommencez !");
    }